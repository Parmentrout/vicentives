import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vicentives.app',
  appName: 'Vicentives',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
